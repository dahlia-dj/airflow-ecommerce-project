# Image Airflow personnalisee incluant les dependances du projet
# (pandas, pyarrow, pymongo) en plus de l'image officielle Apache Airflow.

FROM apache/airflow:2.9.3-python3.11

COPY requirements.txt /requirements.txt

USER airflow
RUN pip install --no-cache-dir --user -r /requirements.txt
